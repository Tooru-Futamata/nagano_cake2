<!--配送先登録/一覧画面-->
<div class="container">
  <div class="row">
	<h2>配送先一覧</h2>
	<table class="table table-hover table-inverse">
      <div class="col-4">
  	    <thead>
  		  <tr class="info">
            <th>郵便番号</th>
            <th>住所</th>
            <th>宛名</th>
            <th colspan="2"></th>
          </tr>
        </thead>
  		<tbody>
  		  <% @addresses.each do |address| %>
  		    <tr>
              <td><%= address.postal_code %></td>
              <td><%= address.address %></td>
              <td><%= address.name %></td>
              <td><%= link_to "編集する", edit_address_path(address), class: "glyphicon glyphicon-pencil btn-sm btn-primary" %></td>
        	  <td><%= link_to "削除する", address_path(address), method: :delete, data: {confirm: "本当に削除してもよろしいですか？"}, remote: true, class: "glyphicon glyphicon-trash btn-sm btn-danger destroy_book_#{address.id}" %></td>
        	</tr>
          <% end %>
        </tbody>
      </div>
    </table>
    <h3>配送先新規登録</h3>
    <% if @address.errors.any? %>
      <%= @address.errors.count %>件のエラーが発生しました
      <% @address.errors.full_messages.each do |message| %>
        <%= message %>
      <% end %>
    <% end %>
    <%= form_with model:@address, local:true do |f| %>
      <div class="addres col-xs-10">
        <div class="form-group col-xs-12">
          <%= f.label :"郵便番号(ハイフンなし)" ,class: "col-xs-3 control-label" %>
          <%= f.text_field :postal_code, class: "col-xs-4 form-control-static postal_code" %>
        </div>

        <div class="form-group col-xs-12">
          <%= f.label :"住所" ,class: "col-xs-3 control-label" %>
          <%= f.text_field :address, class: "col-xs-7 form-control-static address" %>
        </div>

        <div class="form-group col-xs-12">
          <%= f.label :"宛名" ,class: "col-xs-3 control-label" %>
          <%= f.text_field :name, class: "col-xs-4 form-control-static name" %>
        </div>
        <%= f.submit '登録する' ,class: "col-xs-2 pull-right btn-lg btn-success " %>
      </div>
    <% end %>
  </div>
</div>